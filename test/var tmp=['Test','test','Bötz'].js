const names = ['Test','test','Bötz'];

names.sort();
/*tmp.sort(function(a,b) {
    a = a.toLowerCase();
    b = b.toLowerCase();
    if( a == b) return 0;
    return a < b ? -1 : 1;
});*/

console.log(names);

